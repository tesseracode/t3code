# Feature Request: Wire up Copilot SDK context window visualization and token tracking. Surface token usage from session.usage_info in the context window meter component so users can see consumption, compaction thresholds, and remaining capacity. session.usage_info is already mapped but its data may not fully propagate to the UI meter.

**Slug**: `copilot-context-window`
**Created**: 2026-04-22T10:44:43Z

## Description

Wire up Copilot SDK context window visualization and token tracking. Surface token usage from session.usage_info in the context window meter component so users can see consumption, compaction thresholds, and remaining capacity. session.usage_info is already mapped but its data may not fully propagate to the UI meter.
