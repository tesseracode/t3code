{
  "feature": "copilot-cross-platform-build",
  "version": 1,
  "description": "Force-install target platform Copilot binary via npm during cross-compilation.",
  "operations": [
    {
      "id": "01",
      "type": "replace-in-file",
      "path": "scripts/build-desktop-artifact.ts",
      "search": "  // electron-builder is filtering out stageResourcesDir directory in the AppImage for production\n  yield* fs.copy(stageResourcesDir, path.join(stageAppDir, \"apps/desktop/prod-resources\"));\n\n  const stagePackageJson: StagePackageJson = {\n    name: \"t3code\",\n    version: appVersion,",
      "replace": "  // electron-builder is filtering out stageResourcesDir directory in the AppImage for production\n  yield* fs.copy(stageResourcesDir, path.join(stageAppDir, \"apps/desktop/prod-resources\"));\n\n  // Inject Copilot SDK platform binaries into staged dependencies.\n  // bun only installs optional deps for the host OS, so for cross-compilation\n  // we need to add the target platform package as a direct dependency before install.\n  const copilotPlatformMap: Record<string, Record<string, string[]>> = {\n    mac: {\n      arm64: [\"@github/copilot-darwin-arm64\"],\n      x64: [\"@github/copilot-darwin-x64\"],\n      universal: [\"@github/copilot-darwin-arm64\", \"@github/copilot-darwin-x64\"],\n    },\n    linux: {\n      arm64: [\"@github/copilot-linux-arm64\"],\n      x64: [\"@github/copilot-linux-x64\"],\n    },\n    win: {\n      arm64: [\"@github/copilot-win32-arm64\"],\n      x64: [\"@github/copilot-win32-x64\"],\n    },\n  };\n  const copilotPlatformPackages = copilotPlatformMap[options.platform]?.[options.arch] ?? [];\n  const copilotVersion =\n    resolvedServerDependencies[\"@github/copilot-sdk\"] ? \"^1.0.34\" : undefined;\n  const copilotPlatformDeps: Record<string, string> = {};\n  if (copilotVersion) {\n    for (const pkg of copilotPlatformPackages) {\n      copilotPlatformDeps[pkg] = copilotVersion;\n    }\n  }\n\n  const stagePackageJson: StagePackageJson = {\n    name: \"t3code\",\n    version: appVersion,"
    },
    {
      "id": "02",
      "type": "replace-in-file",
      "path": "scripts/build-desktop-artifact.ts",
      "search": "    })`bun install --production`,\n  );\n\n  // Ensure the Copilot SDK platform binary is installed for the target platform.\n  // bun only installs optional deps for the host OS, so when cross-compiling\n  // (e.g., building a Windows installer on macOS) the target binary is missing.\n  const copilotPlatformMap: Record<string, Record<string, string[]>> = {\n    mac: {\n      arm64: [\"@github/copilot-darwin-arm64\"],\n      x64: [\"@github/copilot-darwin-x64\"],\n      universal: [\"@github/copilot-darwin-arm64\", \"@github/copilot-darwin-x64\"],\n    },\n    linux: {\n      arm64: [\"@github/copilot-linux-arm64\"],\n      x64: [\"@github/copilot-linux-x64\"],\n    },\n    win: {\n      arm64: [\"@github/copilot-win32-arm64\"],\n      x64: [\"@github/copilot-win32-x64\"],\n    },\n  };\n  const copilotPlatformPackages = copilotPlatformMap[options.platform]?.[options.arch] ?? [];\n  if (copilotPlatformPackages.length > 0) {\n    yield* Effect.log(\n      `[desktop-artifact] Installing Copilot SDK platform binaries: ${copilotPlatformPackages.join(\", \")}`,\n    );\n    yield* runCommand(\n      ChildProcess.make({\n        cwd: stageAppDir,\n        ...commandOutputOptions(options.verbose),\n        shell: process.platform === \"win32\",\n      })`bun add ${copilotPlatformPackages.join(\" \")}`,\n    );\n  }\n\n  const buildEnv: NodeJS.ProcessEnv = {",
      "replace": "    })`bun install --production`,\n  );\n\n  // Force-install Copilot platform binaries for the target platform.\n  // bun skips packages with os/cpu restrictions that don't match the host,\n  // so cross-compilation needs npm --force to bypass the os check.\n  if (copilotPlatformPackages.length > 0) {\n    yield* Effect.log(\n      `[desktop-artifact] Force-installing Copilot platform binaries: ${copilotPlatformPackages.join(\", \")}`,\n    );\n    for (const pkg of copilotPlatformPackages) {\n      const version = copilotPlatformDeps[pkg] ?? \"latest\";\n      yield* runCommand(\n        ChildProcess.make({\n          cwd: stageAppDir,\n          ...commandOutputOptions(options.verbose),\n          shell: process.platform === \"win32\",\n        })`npm install ${pkg}@${version} --no-save --force`,\n      );\n    }\n  }\n\n  const buildEnv: NodeJS.ProcessEnv = {"
    }
  ]
}